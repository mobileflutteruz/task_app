

import 'package:flutter/material.dart';

class DarslarPage extends StatelessWidget {
  const DarslarPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Darslar Page"),
      ),

    );
  }
}
